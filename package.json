{
  "name": "startup-search-backend",
  "version": "1.0.0",
  "description": "Backend for Startup Search Engine using FastAPI",
  "engines": {
    "python": "3.9"
  },
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}